# PostgreSQL数据库配置 (用于市场监控指标)
# 如果不配置，相关指标将显示默认值0

# 数据库主机地址
DB_HOST=your_postgres_host

# 数据库端口
DB_PORT=5432

# 数据库名称
DB_NAME=your_database_name

# 数据库用户名
DB_USER=your_username

# 数据库密码
DB_PASSWORD=your_password

# SSL模式 (require/disable)
SSL_MODE=require

# 使用说明:
# 1. 复制此文件为 .env
# 2. 填写实际的数据库连接信息
# 3. 在启动脚本中添加: export $(cat .env | xargs)